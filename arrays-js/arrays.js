// let arr=[5,6,9,"raqib",null];
// console.log(arr);

// for each loop to access every element obne by one
// arr.forEach((element,key)=> {
//     console.log(element,key);
// });

// for of loop to access elements in array one by one
// for (const x of arr) {
    
//     console.log(x);
// }

// converting the array copy to a string
// console.log(arr.toString());

// position from deletion should be started and number of elements to be removed from that position
// console.log(arr.splice(2,3));

// deletes the specified element but array size remains
// delete arr[4];

// console.log(arr,arr.length);

// delete arr;
// console.log(arr,arr.length);


let nums=[3,2,5,8,1,15,11];

// sorting the array
// nums.sort((a,b)=>{
//     return b-a;
// });

// console.log(nums);

// map performs operation on each element
// console.log(nums.map((x)=>{
// return x**2;
// }));

// it filters the array elements on a specific condition
// here it will filter elements when they are > 7
// console.log(nums.filter(x=>{
//     return x > 7;
// }));

// reduce operates on all the elements and gives the final answer
// here it calculates the sum
// console.log(nums.reduce((x,y)=>{
//     return x+y;
// }));